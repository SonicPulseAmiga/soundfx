(IF (= @LANGUAGE "français")
(
	(SET #bad-kick				"SoundFX demande au moins le Kickstart 3.0 !")
	(SET #bad-installer			"SoundFX demande l'installer version >= 43.0 !\nCe script peut être trouvé sur Aminet dans util/misc/Installer43.3.lha.")
	(SET #welcome-messy1		"\n\nBienvenue dans l'installation de SoundFX !\nCes scripts vont copier tous les fichiers dont vous avez besoin pour démarrer SoundFX de votre disque dur.")
	(SET #welcome-messy2		"Ce script va enregistrer vos choix de façon permanente.\n Si vous redémarrez le script en mode « Débutant » ou « Initié », vos sélections de la présente session seront rappelées sans vous interroger. Vous ne serez interrogés que si une nouvelle sélection est requise.\nSi vous voulez modifier des options, démarrez le script en mode « Expert ». Ceci aura pour effet de reposer toutes les questions, mais vos anciennes préférences seront proposées.")
	(SET #choose-dest			"Veuillez choisir le répertoire de destination !\nUn répertoire appelé SFX va être automatiquement créé!\nVeuillez vérifiez que vous avez au moins 5.0-10.0 Mo d'espace libre sur votre disque dur !")
	(SET #choose-dest-help		"Ici vous pouvez choisir l'emplacement sur votre DD où SFX doit être installé")
	(SET #copy-loader			"Copie des modules de lecture (loadermoduls)...")
	(SET #copy-operators		"Copie des modules opérateurs (operatormoduls)...")
	(SET #copy-player			"Copie des modules d'écoute (playermoduls)...")
	(SET #copy-rexx				"Copie des scripts AREXX (arexxscripts)...")
	(SET #copy-saver			"Copie des modules de sauvegarde (savermoduls)...")
	(SET #copy-executable		"Copie de l'executable...")
	(SET #copy-fonts			"Copie des polices...")
	(SET #copy-libs				"Copie des bibliothèques...")
	(SET #copy-envelopes		"Copie des enveloppes...")
	(SET #copy-presets			"Copie des réglages par défaut...")
	(SET #copy-workshop			"Copie des fichiers d'initiation...")
	(SET #copy-icons			"Copie des icônes...")
	(SET #copy-gfx				"Copie des éléments graphiques...")
	(SET #copy-text				"Copie des textes...")
	(SET #copy-guides			"Copie des guides...")
	(SET #copy-keyfile			"Copie du fichier clé (Keyfile)...")
	(SET #copy-catalogs			"Copie des catalogues...")
	(SET #copy-help				"Les fichiers respectifs vont être copiés sur votre disque dur.")
	(SET #copy-goodies			"Copie des programmes tiers...")
	(SET #choose-binary			"Veuillez choisir la version à installer pour votre processeur !")
	(SET #choose-binary-help	"Ces versions sont optimisées pour l'usage combiné du cpu et du fpu.")
	(SET #choose-language		"Veuillez choisir la langue de la la documentation !")
;NEW
	(SET #choose-language-help	"In which language would you like the documentation files to be installed?")
	(SET #choose-docfiles		"Please choose which documentation files you would like to install additionally to the html-files!")
	(SET #choose-docfiles-help	"The documentation comes as pdf/ps for printing as well. There are version formatted for a4 and for letter paper.")
;NEW
	(SET #choose-icon			"Veuillez choisir le type d'icône que vous voulez installer !")
	(SET #choose-icon-help		"La version standard est faite pour un WB en 4 couleurs.\nLes versions MagicWB et Iconographics demandent au moins 8 couleurs.\nLes versions NewIcon requièrent que le logiciel NewIcon soit installé.")
	(SET #choose-keypath		"Veuillez choisir le répertoire où le keyfile doit être installé !")
	(SET #choose-fontpath		"Veuillez choisir le répertoire où les polices doivent être installées !")
	(SET #choose-path-help		"Vous pouvez choisir n'importe lequel de ces endroits ! SFX cherchera tous ces endroits.")
	(SET #choose-instmode		"SFX est déjà installé ! Que dois je faire ?")
	(SET #choose-instmode-help	"En mode « backup » (sauvegarde) le vieux répertoire sera renommé en SFX_old.\nEn mode « update » (mise à jour), les fichiers existants seront remplacés.")
	(SET #copykey-fail			"La copie du fichier clé (keyfile) a échoué !")
	(SET #exit-messy			"\n\nSFX a été installé avec succès.\nLisez SVP le guide, pour apprendre comment travailler avec le programme.")
	(SET #orderform				"Enregistrement.txt")
	(SET #catalog				"français")
	(SET #readme				"ReadMe#?")
	(SET #requires-identify		"SoundFX peut désormais utiliser la identify.library >=13.0, mais celle-ci n'est pas installée ou est trop ancienne.")
	(SET #requires-openurl		"SoundFX a désormais besoin de l'openurl.library >=3.0, mais celle-ci n'est pas installée ou est trop ancienne.")
	(SET #requires-popupmenu	"SoundFX peut désormais utiliser la popupmenu.library >=9.0, mais celle-ci n'est pas installée ou est trop ancienne.")
))
